import type { PromptSort } from "@/schemas/promptSchemas";

export const DEFAULT_PROMPT_SORT: PromptSort = "updated_desc";
export const DEFAULT_PROMPT_LIMIT = 20;
export const DEFAULT_PROMPT_OFFSET = 0;